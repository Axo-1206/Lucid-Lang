i planning to change luc to a newer design

currently we have a lot of things, statements: if/else, while, for, ... and type complexity, the problem i want to mention is the complier is getting too complex, we need to simplify it, instead of adding more stuff to the compiler we need to define critical blocks allow user to define library easier. I also borrow some syntax from haskell for this new design

to design this we should understand the basic of programming, in nutshell all we do are 
1. define a data structure to store data like classes, struct, ...
2. define action via function like perform computation on the data