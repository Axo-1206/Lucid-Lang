mod Player -- tests/oop/Player.luc



pub fn getName(p Player) string {
    return p.name
}

-- A "Static" constructor style function
pub fn new() Player {
    -- In the future, this will contain object initialization
}

-- A "Method" using a Receiver
pub fn (self: Player) dosomething() {
    -- Logic for the player
}