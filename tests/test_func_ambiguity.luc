package tests

struct A = {
    a int = 5
}

from A = {
    
}

export const main () int = {
    let existingFunction (a int) int = {
        return a + 1
    }

    -- this should be an error? because the variable f is already assigned by the existingFunction, so the following function body is wrong
    let f (a int) int = existingFunction 
    
    return 0
}
